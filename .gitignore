# Local runtime data can contain account records, password hashes, sessions,
# cards, and uploaded images. Keep it on this computer.
/data/
/upload/
/github-release/

# Install dependencies from package-lock.json after cloning.
/node_modules/

# Local configuration and runtime output
.env
.env.*
!.env.example
*.log
npm-debug.log*

# Downloadable archives and operating system files
/*.zip
.DS_Store
Thumbs.db
